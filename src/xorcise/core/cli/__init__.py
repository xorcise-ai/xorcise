"""xorcise.core.cli — the ``xorcise`` command.

LAYER: cli (composition root for the command line). A THIN client: holds no
logic beyond the REST client (rest_client.py) and the local lifecycle driver.
"""
