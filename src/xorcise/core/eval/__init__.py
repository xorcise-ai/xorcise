"""xorcise.core.eval — the grader (50/50 verdict).

LAYER: PART-ISLAND. Imports only contracts + shared kernel. Never another part.

Deterministic half landed: operations + resolvers + run_check + grade_deterministic.
The BYOM judge half and the 50/50 combine are still to come.
"""
