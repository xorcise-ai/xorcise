"""xorcise.core — the XORCISE server + package root.

LAYER: APPLICATION (and the internal-namespace root). The sole conductor of the
part-islands, reached only through typed clients.
ZERO side-effect imports.
"""
