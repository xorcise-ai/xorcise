"""xorcise.core.orchestration — the conductor: drives peers via typed clients ONLY.

LAYER: APPLICATION (domain module). ports.py holds in-process ABCs (beside owner); concrete
adapters live in clients/.
"""
