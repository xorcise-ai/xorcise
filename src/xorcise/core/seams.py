"""xorcise.core.seams — the ONLY file that imports ``premium``.

LAYER: SEAM. For each seam, reads config and returns the OSS default OR the
premium client. Stub: returns OSS defaults only (premium wiring not yet built).
"""

from __future__ import annotations
