# PR assets — design-system conformance

Screenshots referenced from PR "feat(frontend): apply the XORCISE design system across
the console". Kept on an **orphan branch** so `main` carries no binaries; safe to delete
once that PR is merged and the images are no longer needed in its description.

Captured from the built static export served by `xorcise serve`, at 1440x900 (and 375px
for the responsive shot), against a local database with 3 missions installed and 2 runs.
No credentials are configured in the environment shown (`judge.configured: false`), so
every key field renders its placeholder.
